{{- if .Values.analyzer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kyntra.fullname" . }}-analyzer
  labels:
    {{- include "kyntra.labels" . | nindent 4 }}
    app.kubernetes.io/component: analyzer
spec:
  {{- if not .Values.analyzer.autoscaling.enabled }}
  replicas: {{ .Values.analyzer.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "kyntra.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: analyzer
  template:
    metadata:
      labels:
        {{- include "kyntra.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: analyzer
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: analyzer
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.analyzer.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.analyzer.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "kyntra.fullname" . }}-config
            - secretRef:
                name: {{ .Values.secrets.existingSecret | default (printf "%s-secret" (include "kyntra.fullname" .)) }}
          env:
            - name: PORT
              value: {{ .Values.analyzer.service.port | quote }}
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.analyzer.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyntra.fullname" . }}-analyzer
  labels:
    {{- include "kyntra.labels" . | nindent 4 }}
    app.kubernetes.io/component: analyzer
spec:
  type: {{ .Values.analyzer.service.type }}
  ports:
    - port: {{ .Values.analyzer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kyntra.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: analyzer
{{- if .Values.analyzer.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "kyntra.fullname" . }}-analyzer
  labels:
    {{- include "kyntra.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "kyntra.fullname" . }}-analyzer
  minReplicas: {{ .Values.analyzer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.analyzer.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.analyzer.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
{{- end }}
